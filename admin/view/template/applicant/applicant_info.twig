{{ header }}{{ column_left }}
<div id="content">
  <div class="page-header">
    <div class="container-fluid">
      <div class="pull-right">
        
        <a href="{{ cancel }}" data-toggle="tooltip" title="{{ button_cancel }}" class="btn btn-default"><i class="fa fa-reply"></i></a></div>
      <h1>{{ heading_title }}</h1>
      <ul class="breadcrumb">
        {% for breadcrumb in breadcrumbs %}
        <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
        {% endfor %}
      </ul>
    </div>
  </div>

  <div class="container-fluid">
    {% if error_warning %}
    <div class="alert alert-danger alert-dismissible"><i class="fa fa-exclamation-circle"></i> {{ error_warning }}
      <button type="button" class="close" data-dismiss="alert">&times;</button>
    </div>
    {% endif %}
 
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">
          <i class="fa fa-pencil"></i>
            Form Details 
        </h3>
       
      <div class="panel-body">
      <table id="table" class="table table-striped">
        <thead>
        <tr>
            <th>Label</th>
            <th>Content</th>
        </tr>
        </thead>
        <tbody>
          {% for names in name %}
            <tr>
              <td>{{ names.name }}</td>
            </tr>
            {% endfor %}

        {% for temps in temp %}
            <tr>
                <td>{{ temps.label }}</td>
                <td>{{ temps.content }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </div>
  </div>
</div>




<div class="panel panel-default" id="remarks">
  <div class="panel-heading">
    <h3 class="panel-title">
      <i class="fa fa-pencil">
      </i>
      Remarks
    </h3>
  </div>
  <div class="panel-body">
    <div class="form-group">
      <textarea id="input-remarks" name="remarks" class="form-control"></textarea>
    </div> 
    <p align="right">
      <button type="submit" id="remarks" name="remarks"  class="btn btn-success" >    </p>
  </div>
</div>
</div>
<!-- 
<div class="container-fluid">
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title">
        <i class="fa fa-pencil"></i>
          Approval Log
      </h3>
     
    <div class="panel-body">
    <table id="table" class="table table-striped">
      <thead>
      <tr>
          <th>Date Approved</th>
          <th>Approved By</th>
          <th>Status</th>
      </tr>
      </thead>
      <tbody>
        {% for admins in admin %}
            <tr>
              <td>{{ admins.admin }}</td>
            </tr>
            {% endfor %} -->

        <!-- {% for temps in temp %}
            <tr>
                <td>{{ temps.label }}</td>
                <td>{{ temps.content }}</td>
            </tr>
        {% endfor %} -->
      <!-- </tbody>
  </table>
  </div>
</div>
</div>
</div>
</div> -->



<div class="panel-body" id="pbutton">   
<form method='post' name='approval'>
  <input type='hidden' name='applicant_id' value={{applicant_id}}>
  <p align="right">
<button type="submit" id="approve" name="approve"  class="btn btn-success" >
  <i class="fa fa-check">Approve</i>
  </button>
  <form method="post" name="rejection">
  <button type="submit" id="reject" name="reject"  class="btn btn-danger" >
    <i class="fa fa-close">Reject</i>
    </button>
  </form>
</p>
</form>
