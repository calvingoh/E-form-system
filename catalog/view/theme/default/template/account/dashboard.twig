{{ header }}
<div id="account-department" class="container">
  <ul class="breadcrumb">
    {% for breadcrumb in breadcrumbs %}
    <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
    {% endfor %}
  </ul>
  <div class="row">{{ column_left }}
    {% if column_left and column_right %}
    {% set class = 'col-sm-6' %}
    {% elseif column_left or column_right %}
    {% set class = 'col-sm-9' %}
    {% else %}
    {% set class = 'col-sm-12' %}
    {% endif %}
    <div id="content" class="{{ class }}">{{ content_top }}
      <br/>
      
      <div class="dashboard-nav">
      <!-- <div class="panel panel-default">
        <div class="panel-heading">
          <h1 class="panel-title"><i class="fa fa-list"></i> {{ column_recent_activity }}</h1>
        </div>
        <div class="panel-body">
        <div class="table-responsive">
          <table class="table table-bordered table-hover">
            <thead>
              <tr>
                  <td class="text-left">{{ column_activity }}</td>
                  <td class="text-left">{{ column_department }}</td>
                  <td class="text-left">{{ column_action }}</td>
                 </tr>
            </thead>
            <tbody>
            
              {% if activtities %}
              {% for activity in activities %}
              <tr>
                <td class="text-left">{{ activity.activity_name }}</td>
                <td class="text-left">{{ department.department_name }}</td>
                <td class="text-right"><a href="{{ department.edit }}" data-toggle="tooltip" title="{{ button_edit }}" class="btn btn-primary"><i class="fa fa-eye"></i></a></td>
              </tr>
              {% endfor %}
              {% else %}
              <tr>
                <td class="text-center" colspan="6">{{ text_empty_activity }}</td>
              </tr>
              {% endif %}
            </tbody>  
          </table>
      </div>
      <div class="row">
        <div class="col-sm-6 text-left">{{ pagination }}</div>
        <div class="col-sm-6 text-right">{{ results }}</div>
      </div>
      {{ content_bottom }}</div>
    </div>
    <br/>
    <br/> -->

    <div class="panel panel-default">
        <div class="panel-heading">
          <h1 class="panel-title"><i class="fa fa-file"></i> {{ column_form_approved }}</h1>
        </div>
        <div class="panel-body">
        <div class="table-responsive">
          <table class="table table-bordered table-hover">
            <thead>
              <tr>
                  <td class="text-left">{{ column_forms }}</td>
                  <td class="text-left">{{ column_department }}</td>
                  <td class="text-left">{{ column_status }}</td>
                  <td class="text-right">{{ column_action }}</td>
                 </tr>
            </thead>
            <tbody>
            
              {% if approves %}
              {% for approve in approves %}
              <tr>
                <td class="text-left">{{ approve.eform_id}}</td>
                <td class="text-left">{{ approve.department}}</td>
                <td class="text-left">{{ approve.status }}</td>
                <td class="text-right"><a href="{{ approve.edit }}" data-toggle="tooltip" title="{{ button_edit }}" class="btn btn-primary"><i class="fa-fa-eye"></i></a></td>
              </tr>
              {% endfor %}
              {% else %}
              <tr>
                <td class="text-center" colspan="6">{{ text_empty_approved_forms }}</td>
              </tr>
              {% endif %}
            </tbody>  
          </table>
      </div>
      <div class="row">
        <div class="col-sm-6 text-left">{{ pagination }}</div>
        <div class="col-sm-6 text-right">{{ results }}</div>
      </div>
    <!--   <div class="buttons clearfix">
        <div class="pull-right"><a href="{{ continue }}" class="btn btn-primary">{{ button_continue }}</a></div>
      </div> -->
      {{ content_bottom }}</div>
    </div>
    </div>
    </div>
    {{ column_right }}</div>
</div>

<!-- {{ footer }} -->